5/27
.objフォーマットに変換しようとする→うまく行かなかった
blenderで.mqoインポートのアドオンを実装→.mqoをインポートできるようになった。
blenderでscriptingを試す。→terminalからblenderを実行、レンダリングまで可能

blender内のコードでmqoファイルをインポートできた
vscodeのpythonでblenderを動かせた。
→blender内のコードでmqoファイルを作成するプログラムを動かせない。
→vscodeのpythonでmqoファイルをインポートできない。
	→撮影した画像でmqoファイルを作り、blenderにインポートするところまでを1つのプロジェクト（1回の実行）で行いたい。
	
5/28
ターミナルからblenderを実行→blenderを外部pythonファイルで動かせた。
use_cutなどのTrue,Falseをcreate_MQOからmove_face_modelで受け取ろうとする。→getterやfrom importを試したがうまくいかず
それぞれにコマンドライン引数として-cを用意し、これによりカットするかを決められるようにした。
口の形をルーブで表示させるbashスクリプトを書いた
→create, moveに口の形をコマンドラインで渡し、ループさせたい。→createのコマンドライン実装、最初のif文実装、最初のGUIに対応つけ→コード全体をコマンドラインに対応させる

5/29
シェルスクリプトをルートディレクトリから実行できるようになった
aiueonそれぞれの形で撮影とモデルの保存が一度にできるようになった。

6/3
ワイヤーで保存できるようにした
モデルごとに.mqoを保存できるようにした
テクスチャ2枚を入力として、1枚目から2枚目までのモデルを作成できた（分割数指定,現在5でテスト）
→moveのファイルで入力や、ファイル名を対応させる。今は25,51,62,77がなぐり書き
→createのmainとも対応させる

6/4
main, render(元move), change, createを書き換え、引数のやり取りを実装
ターミナルから全変化パターンのモデルを生成
→課題：テクスチャの違和感、「う」「お」の違和感

6/6
「う」「お」の違和感→実写だとうまくいく
テクスチャの違和感を解決したい→メッシュに対応したテクスチャ座標を線形に変化、テクスチャを線形にブレンド
→課題：テクスチャの口の位置がずれると違和感

6/7
ブレンドテクスチャの違和感→深層学習で2枚の入力画像間を補間できるようにした。
→補間回数を3回（補間フレーム7枚）に指定し、出力。めちゃくちゃきれいにできた。

6/10
正規化ブレンドの実装→鼻を中心に正規化→顔のサイズは未実装
実行時間の精査→AI, mediapipe(正規化), renderにbashスクリプトを分けた
→どちらにせよ時間がかかる→nvidiaを実装したい
→ちょうど今日香ちゃんのPCが空いたので、そのPCに移す
→nvidia、cuda、cuDNNのインストール→動かない！！！

6/11
nvidia, cuda, cuDNN, tensorflow, tensorRT, bazel、全てバージョン確認して再インストールし、ビルドまでこぎつけた。
→ビルド後の.whlが作成されない。そもそもbazel-binというディレクトリがない。

6/12
nvidia, bazel, clangを最新バージョンのまま残し、tensorflow, torch, cuda, cudnnを最新でインストール
→tensorRTとの対応はこれから

正規化ブレンドをサイズに対応→なんか変
実行時間の原因を発見→mainを通して実行していた（app？）
→mainを通さずchange_MQOで完結させた→めっちゃ速い！！！！

6/13
tensorRTのためのtensorflowビルドができない→bazel-binディレクトリが作られない
dockerを試す→なにがなんだかわからない
諦め

6/14
実行前のモデル作成は時間がかかってもOK→CPUで動く深層学習を利用
表示するモデルがCreate_MQOとLoad_MQOで共通→1度の実行で、作成したモデル1つしか表示できない
→表示するモデルをLoadで完結させた→モデル名を変更するだけで表示を変えられるようになった
モデル表示の際、同じウィンドウのままにしたい→Appにボタンを追加
→好きなタイミングでモデルを変えられるようになった！

6/19
Appとinterpolator_cliにuse_cutを渡せるようにした
モデルを動的に表示できるようにした

6/24
モデルを都度読み込みから、都度計算に変更したい→最初に6モデル読み込んで、都度テクスチャ張りと座標計算
→6モデルの読み込みが出来ない→テクスチャが変更されない。でもテクスチャ名は変わっている

6/25
textureIDsがおかしかった→書き換えるとうまく反映された！！
→最初に6モデル読み込み、表示の際はmodelを渡すと、setするdataを計算し表示してくれる。
→実行時間約1/4になった
補間モデルの計算→テクスチャだけ反映させたい→またバインドがうまく行かない

6/26
loadMQOインスタンスのtexだけを変更していた→もう１階層下のmaterialインスタンスのload_textureを実行
→うまくいった！！！
landmark_nomalizeの変更→うまくいった
uvの変更→だめだった（なんか惜しい気はする）

6/27
頂点座標が変わらない

6/28
landmark_nomalizeは頂点座標の変更には直接関係ない→mesh.verticesを書き換えなければいけない
→めんどくさいのでmqoファイルを作成する方に変更する。

7/1
mesh.verticesを書き換えたらうまく行った→現在の仕様で進める
テキストから簡単な動的再現を実装

7/2
破裂音、w、促音を再現

7/3
ループにかかる時間が思ったより長い→switch_intervalが意味をなさない
roop_min: 0.01267242431640625q
roop_max1: 0.18092584609985352
roop_max2: 0.07660961151123047
→interval=0にして、表示フレームを半分にして再現
stnyzjを再現
んをNにしたがnow_mouthでおかしくなる

7/4
roop平均（switch_interval=0時のswitch_time）が0.03
→switch_intervalを0.02で固定→0.03だと上回りすぎて、0だと早すぎる
now_mouthの調整
1音ずつの変化時間をだいたい揃えられた

7/5
促音の微調整

7/8
ゼミ資料作成、テキスト解析の調整

7/9
ゼミ資料作成

7/10
ゼミ資料作成、switch_intervalで制御できるように変更

7/11
ゼミ資料作成

7/16
モデル表示時のmaterialをアルファに対応させた
blenderでアルファペイントを試す→線形にしか変化できない
→結局境界線がくっきりする

7/17
顔の輪郭を検出し、アルファ範囲を決められた→隙間ができる

7/18
顔の輪郭検出時に使うグラフを表示させた

7/19
輪郭のアルファ値変換が出来た

7/22
輪郭透過状態での動的変化

7/23
断面の透過を作成→全体的に変になった

7/25
透過の範囲を広くした→テクスチャの明るさの違和感もなくなった

8/21
デモの準備→名前ごとにデータを保存できるようになった、テキストを途中で変えられるようになった

8/22
デモの準備→テクスチャ撮影時のラグを解消

10/3
音声入力を試す→ウェブカメラでマイクの入力を確認
→精度が悪い（区切られてしまう）

10/4
音声入力を試す→高精度で実装できたが、ラグがある→ゼミにてラグは許容された

10/7
バックグラウンドで音声認識ができた（同期処理）
カメラを起動しながら音声認識で発話内容を生成できるようになった
